{
  "name": "@netlaunch/database",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "prisma generate && tsc",
    "dev": "tsc --watch",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio",
    "db:seed": "tsx src/seed.ts",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@prisma/client": "^5.21.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@netlaunch/config": "workspace:*",
    "@netlaunch/shared": "workspace:*",
    "@types/node": "^22.8.1",
    "prisma": "^5.21.0",
    "tsx": "^4.19.1",
    "typescript": "^5.6.3"
  }
}
